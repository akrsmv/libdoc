import { IClaims, IIdentity } from "@incta/ddb-model";
import { queryBe } from "../../domain-context/items/queryItems/queryBe";

export const start = async (queryBe: queryBe, identity: Partial<IIdentity<Partial<IClaims>>> | null): Promise<void> => {
    
}

export const validateStart = async (queryBe: queryBe, identity: Partial<IIdentity<Partial<IClaims>>> | null): Promise<void> => {
    
}
